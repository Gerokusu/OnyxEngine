/**
 * Animator - Represents an animation manager.
 *
 * @param  {String} state       the current state.
 * @param  {String} animations  the animations primary id array.
 */
function Animator(state, animations)
{
    this.state = state;
    this.animations = animations;
}
