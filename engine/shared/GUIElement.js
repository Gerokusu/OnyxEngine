/**
 * GUIElement - Represents a GUI Element.
 *
 * @param {String} texture the element background texture.
 * @param {String} textarea the text area rectangle.
 */
function GUIElement(texture, textarea)
{
    this.texture = texture;
    this.textarea = textarea;
}
